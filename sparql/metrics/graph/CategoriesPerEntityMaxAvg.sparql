PREFIX clonto: <http://myCLOnto.de/> 
SELECT (MAX(?CategoryPerEntity) as ?max) (AVG(?CategoryPerEntity) as ?avg)
WHERE{
	{SELECT ?entity (COUNT(?category) as ?CategoryPerEntity)
	WHERE {
		?category clonto:hasEntity ?entity .
	}
	GROUP BY ?entity
}
}	